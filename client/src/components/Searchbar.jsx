import React, { Component } from 'react';

// Input to search database for specific presentation
class Searchbar extends Component {

  render () {
    return (
      <input type='text' value='Search old slides'/>
    );
  };
};

export default Searchbar;
