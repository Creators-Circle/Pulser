import React from 'react';

// component for displaying the summary detail
const SummaryInfoBoxDetail = (props) => {
  return (
    <li>{props.detail}</li>
  );
};

export default SummaryInfoBoxDetail;
